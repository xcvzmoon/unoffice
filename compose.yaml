services:
  unoffice:
    image: unoffice:${IMAGE_TAG:-latest}
    container_name: unoffice
    build:
      context: .
      args:
        UNOSERVER_VERSION: ${UNOSERVER_VERSION:-3.3.2}
    environment:
      PORT: ${PORT:-3000}
    ports:
      - '${PORT:-3000}:${PORT:-3000}'
